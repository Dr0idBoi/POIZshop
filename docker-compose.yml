# docker-compose.yml
version: "3.8"
services:
  bot:
    build: .
    env_file:
      - .env
    secrets:
      - gcp_sa.json
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    command: ["python", "-m", "app"]
secrets:
  gcp_sa.json:
    file: ./gcp_service_account.json
